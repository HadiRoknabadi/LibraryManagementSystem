@using Application.DTOs.Book
@model FilterBookDTO


@{
    ViewData["Title"] = "کتاب ها";
}

<div class="container margin-bottom-10">
    <a asp-controller="Book" asp-action="Books" class="btn btn-success">افزودن کتاب</a>
</div>

<div class="col-lg-4 col-md-4 col-sm-12 col-xs-12 ">
    <div class="well with-header">
        <div class="header bordered-sky">
            فیلتر
        </div>
        <form id="filter-form" asp-controller="Book" asp-action="Books" method="get">
            <input type="hidden" asp-for="PageId" />
            <div class="form-group">
                <label>نام کتاب</label>
                <input asp-for="Title" class="form-control">
            </div>
            <div class="form-group">
                <label>ISBN</label>
                <input asp-for="ISBN" class="form-control">
            </div>
            <hr />
            <h4>مرتب سازی</h4>
            <div class="control-group">
                @foreach (var item in Html.GetEnumSelectList<FilterDataOrder>())
                {
                    <div class="radio">
                        <label>
                            <input type="radio" class="colored-blue" value="@item.Value" name="@nameof(Model.OrderBy)"
                                   @(Convert.ToInt32(item.Value) == Convert.ToInt32(Model.OrderBy) ? "checked" : "")>
                            <span class="text">@item.Text</span>
                        </label>
                    </div>
                }
            </div>
            <button type="submit" class="btn btn-success btn-block">فیلتر</button>
        </form>
    </div>
</div>

<div class="col-lg-8 col-md-8 col-sm-12 col-12 col-xs-12">
    <div class="well with-header with-footer">
        <div class="header bordered-sky">
            @ViewData["Title"]
        </div>
        @if (Model.Books != null && Model.Books.Any())
        {
            <div class="table-scrollable">

                <table class="table table-bordered table-hover text-center">
                    <thead>
                        <tr>
                            <th>
                                شناسه
                            </th>
                            <th>
                                نام کتاب
                            </th>
                            <th>
                                 ISBN
                            </th>
                            <th>
                                دستورات
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var book in Model.Books)
                        {
                            <tr id="ajax-url-item-@book.Id">
                                <td>
                                    @book.Id
                                </td>
                                <td>
                                    @book.Title
                                </td>
                                <td>
                                    @book.ISBN
                                </td>
                                <td>
                                    <a asp-controller="Book" asp-action="EditBook" asp-route-bookId="@book.Id"
                                       class="btn btn-warning btn-xs">ویرایش</a>
                                    <a asp-controller="Book" asp-action="DeleteBook"
                                       asp-route-bookId="@book.Id" ajax-url-button="@book.Id"
                                       class="btn btn-danger btn-xs">حذف</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        }
        else
        {
            <Alert type="warning" message="هیچ اطلاعاتی یافت نشد"></Alert>
        }
        <div class="footer">
            <partial name="_AdminPagingPartial" model="@Model.GetCurrentPaging()" />
        </div>
    </div>
</div>
